#include <bits/stdc++.h>
using namespace std;

vector<int> arr;

pair<int,int> MaxMin(int x, int y){
    if ( y - x <= 1)
        return make_pair(max(arr[x],arr[y]), min(arr[x], arr[y]));

   
}

int main(){
    return 0;
}